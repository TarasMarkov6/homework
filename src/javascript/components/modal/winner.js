import showModal from './modal';

export default function showWinnerModal(fighter) {
    // call showModal function
    let outer = document.querySelector('.arena__root');
    let elem = document.createElement('div');
    elem.classList.add('');
    
}
